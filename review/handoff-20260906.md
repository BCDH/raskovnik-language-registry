# Registry handoff — 2026-09-06

Current state: the six profile blockers are resolved and committed conversion/mirror/backend changes pass validation. Production packaging is blocked on 20 newly exposed ancestor-label approvals. See the packaging implementation checkpoint below; the original handoff is retained as history.

## Original handoff checkpoint

All 25 saved human decisions (24 corrections and one confirmation) are imported without changing their values, notes, or timestamps. The original user export remains byte-identical in the hashed session archive. Agent-reviewed records carry Codex as reviewer. All 234 label additions retain their provenance; profile labels take precedence when an automatically generated structural ancestor coalesces with the same exact language profile. Bookkeeping and the two Shifted Romance nodes retain the documented nonselectable English fallback. Related identifiers remain in structured review payloads and supporting notes, never exact identifier slots.

The import report records 237 applied records and five pending source proposals. Native standards validation additionally exposes the Estonian scope mismatch, bringing the effective release queue to six. None of these six is an unresolved saved human correction.

| Profile | Remaining action |
|---|---|
| `cel-x-gaulish` | The reviewed proposal selects `xtg`; migrate the conversion catalogue and regenerate its provenance-pinned snapshot before importing that canonical identity. |
| `ga-x-old` | The reviewed proposal selects `sga`; requires the same source migration. |
| `hr-x-kajkav` | The reviewed proposal selects `kjv`; requires the same source migration. |
| `lt-x-old` | The reviewed proposal selects `olt`; requires the same source migration. |
| `vel` | The PERSJ source meaning is вељотски (Vegliot), while ISO `vel` and Glottolog `velu1238` identify Dutch Low Saxon Veluws. Correct the source mapping after auditing the intended Vegliot scope. No current PERSJ form uses this catalogue tag. |
| `et` | ISO `est` is the Estonian macrolanguage, while the proposed exact Glottolog node carries ISO `ekk` (Standard Estonian). Resolve generic versus narrower scope before approving an exact alignment. |

Veluws evidence: [Glottolog velu1238](https://glottolog.org/resource/languoid/id/velu1238). Vegliot terminology: [Kovačec, Leksičke podudarnosti veljotskoga i rumunjskoga](https://hrcak.srce.hr/file/265826). Estonian evidence is reproducible from the pinned IANA, ISO 639-3 and Glottolog tables; `exact-iso-scope-mismatch` now exposes the conflict in the generated queue.

The original Wikidata snapshot is unchanged. The new 2026-09-06 snapshot retains its records and adds 28 revision-pinned items selected in the review. Generic Ossetian keeps `Q33968` with broader `osse1245`; Iron keeps `Q2585922` with exact `iron1242`. An exact language-profile QID is not duplicated onto a broader structural ancestor. An explicit blank QID, including the saved `gmh` decision, stays blank.

Validation: registry `make check` passes 81 tests and generated-candidate parity. Import `--check` verifies byte-for-byte idempotence and preservation of all saved human labels on selectable nodes. The full approved subset compiles in memory (377 nodes), with no production artifacts written. Production plan assembly stops at the six-record editorial gate; no release XML, manifest, or XAR has been produced for this handoff.

Workspace validation: all six dictionaries build; PERSJ mirror provenance verifies; the complete backend render suite passes 123 checks. The affected GE.RKMD, MZ.RGJS and ISJ.PERSJ resources match the local builds and backend resource hashes. PERSJ retains 700 `sh`, nine `os`, 16 `ira-x-ossetic`, 29 `gmh`, and 934 `pl` occurrences and its private visibility. Any further tag correction remains conversion-owned; no dictionary source was edited in this handoff.

The all-dictionary parity check found an independent existing VSK.SR homograph-placement difference at the таван cross-reference. The previous generator reproduces the current VSK.SR build byte-for-byte; the difference is not caused by this spacing change and was not synchronized. Backend API/projection, release-integration, five capability-validator scenarios and six capability-manifest scenarios pass. Frontend focused tests pass: 44 tests, 372 assertions. Live eXist deployment and Google Maps verification were not performed.

## Packaging implementation checkpoint — 2026-09-06

The six profile blockers above are resolved. The source conversion is committed at `a258c532c1037b9550d22154ccd9f34cef1a2073`, the guarded mirror export at `a1d159e30f`, and the synchronized backend resource and hashes at `9cf6738`. Exactly 61 Old Irish, 20 Old Lithuanian, and six Gaulish form attributes changed to `sga`, `olt`, and `xtg`. The unused Vegliot catalogue label now maps to `dlm-x-vegliot`. Generic Kajkavian remains `hr-x-kajkav`; its reviewed exact Glottolog alignment has an explicit, evidence-hash-bound exclusion of narrower ISO `kjv`. Generic Estonian retains `et` / `est` / `Q9072`, with broader Finnic alignment `finn1317`. The three retired historical private tags and erroneous `vel` are not active aliases.

The reproducible reconciliation is archived under `review/sessions/2026-09-06-packaging/`. It retains the complete prior ledger, candidate snapshot, blocked report, resolutions, and per-record report with hashes. All 25 saved human decisions and the original session remain unchanged. The ledger has 242 applied review records and six explicitly superseded historical records; superseded proposals are retained as history, never active exact alignments. The generated review page still has 260 rows and all 25 saved decisions.

Full production assembly exposed two additional implementation defects beyond the initial six-profile gate: 77 coalesced profile/ancestor rows disagreed on display metadata, and XML namespace serialization depended on Python import order. Both are corrected and covered by tests. The complete assembled inventory has 380 nodes, 214 tag profiles, and 311 count-free source records. Both enabled dictionaries' 193 used tags resolve against this inventory.

The full native validation also exposed 20 nonselectable ancestors missing Serbian and German labels; these ancestors were absent from the earlier 260-row review. The editorial gate now reports them explicitly as 20 ancestor exceptions, with zero profile exceptions. Generating a deterministic ancestor code no longer implies editorial approval. The [proposed labels](packaging-ancestor-label-proposals-20260906.md) are prepared for one batch approval. Automatic approval review rejected applying these additional translations together with a native fallback-validator change as outside the approved scope; neither was applied. The three previously approved English-only fallbacks remain unchanged. Native Python label validation must subsequently be brought into agreement with those three reviewed fallbacks and the existing Schematron policy, while selectable nodes continue to require all three labels.

No complete production XML, manifest, or XAR has been produced. The incomplete effective plan from the failed build was removed; assembly now validates the generated structure before writing a production plan. After explicit approval of the label proposals, record their blank-only additions with provenance through the reconciliation, regenerate candidates, require a zero-exception editorial gate, run full native compilation and packaging, verify deterministic artifacts and XAR inventory, and refresh this checkpoint. Do not publish or deploy as part of this local handoff.

Completed validation: conversion catalogue, reviewer, decision, compiler, XSLT, UI, advisory-suggestion and full-corpus suites; both committed pipelines through Stage 67 and full/sample manifest parity; guarded exporter/mirror parity; all existing language-count invariants; backend render contracts (123 checks, zero failed), registry projection, evidence (79 assertions), map (24 assertions), release integration, capability validator (five scenarios), and capability manifests (six scenarios). Focused frontend checks pass 54 tests and 2,653 assertions. No live deployment, browser interaction verification, or indexing benchmark was performed.
